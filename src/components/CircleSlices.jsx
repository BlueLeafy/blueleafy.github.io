import { useState } from "react";

function CircleSlices() {
    const totalSlices = 12;
    const [coloredSlices, setColoredSlices] = useState(0); // Tracks the number of slices

    const handleClick = () => {
        if (coloredSlices < totalSlices) {
            setColoredSlzices
        }
    }

    return (
        <div className="circle">

        </div>
    )
}