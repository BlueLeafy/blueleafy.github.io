import HomeProjects from "../components/Projects/HomeProjects";
// Grid for project gallery linkable
// Grid when shrinking will change number of columns not shrink their width

function HomePage() {
    return (
        <>
            <HomeProjects />
        </>
    );
};

export default HomePage;